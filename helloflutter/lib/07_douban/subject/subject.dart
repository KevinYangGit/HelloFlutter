
import 'package:flutter/material.dart';

class KYSubject extends StatelessWidget {
  const KYSubject({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("subject"),
      ),
      body: Center(
        child: Text("subject"),
      ),
    );
  }
}